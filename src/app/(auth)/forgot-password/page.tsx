'use client'
import { useState } from 'react'
import { ArrowLeft, Mail, Send, Zap } from 'lucide-react'
import Link from 'next/link'
import { toast } from 'sonner'
export default function ForgotPasswordPage(){const [email,setEmail]=useState('');const [sent,setSent]=useState(false);return <main className="flex min-h-screen items-center justify-center bg-slate-950 p-6"><div className="w-full max-w-md"><div className="mb-8 flex justify-center items-center gap-3 text-xl font-bold"><span className="primary-icon"><Zap/></span>ShadowSpark</div><div className="panel p-8"><div className="flex size-12 items-center justify-center rounded-xl bg-amber-500/10 text-amber-400"><Mail/></div><h1 className="mt-5 text-2xl font-semibold">Reset your password</h1>{sent?<div className="mt-4 rounded-lg border border-emerald-500/30 bg-emerald-500/10 p-4 text-sm text-emerald-300">Check your email for reset instructions. The link expires in 30 minutes.</div>:<><p className="mt-2 text-sm leading-6 text-slate-500">Enter the email associated with your ShadowSpark account and we&apos;ll send a secure reset link.</p><form className="mt-6 flex flex-col gap-4" onSubmit={e=>{e.preventDefault();setSent(true);toast.success('Reset link sent')}}><label className="field-label">Email address<input required type="email" className="form-control" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></label><button className="primary-button"><Send data-icon="inline-start"/>Send Reset Link</button></form></>}<Link href="/login" className="mt-6 flex items-center gap-2 text-sm text-slate-400 hover:text-amber-400"><ArrowLeft/>Back to sign in</Link></div></div></main>}
