'use client'
import { loginUser } from '@/app/actions/auth'
import { useRouter } from 'next/navigation'
import { useState } from 'react'
import { Check, Eye, EyeOff, KeyRound, Zap } from 'lucide-react'
import Link from 'next/link'
import { PasskeyClient } from '@/components/auth/PasskeyClient'
export default function LoginPage(){const [error,setError]=useState('');const [loading,setLoading]=useState(false);const [show,setShow]=useState(false);const [usePasskey,setUsePasskey]=useState(false);const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setError('');setLoading(true);const result=await loginUser(new FormData(e.currentTarget));if(result.error){setError(result.error);setLoading(false)}else router.push('/dashboard')}return <main className="grid min-h-screen bg-slate-950 lg:grid-cols-2"><section className="hidden flex-col justify-between border-r border-slate-800 p-12 lg:flex"><div><div className="flex items-center gap-3 text-xl font-bold"><span className="primary-icon"><Zap/></span>ShadowSpark</div><div className="mt-28 max-w-lg"><p className="eyebrow">Infrastructure for African lenders</p><h1 className="mt-4 text-5xl font-bold tracking-tight text-slate-100">AI-powered lending infrastructure for Africa.</h1><p className="mt-6 text-lg leading-8 text-slate-400">Automate loan origination, instant KYC verification, and intelligent recovery at scale.</p><ul className="mt-8 flex flex-col gap-4 text-sm text-slate-300">{['WhatsApp-native loan applications','Automated KYC and credit scoring','NDPR-compliant audit trails','Multi-tenant enterprise architecture'].map(item=><li key={item} className="flex items-center gap-3"><Check className="text-amber-400"/>{item}</li>)}</ul></div></div><p className="text-xs text-slate-600">© 2026 ShadowSpark Technologies</p></section><section className="flex items-center justify-center p-6"><div className="w-full max-w-md"><div className="mb-8 text-center lg:hidden"><div className="mx-auto flex w-fit items-center gap-3 text-xl font-bold"><span className="primary-icon"><Zap/></span>ShadowSpark</div></div><div className="panel p-8"><div className="text-center"><h2 className="text-2xl font-semibold">Welcome back</h2><p className="mt-2 text-sm text-slate-500">Sign in to your lending operations workspace.</p></div><div className="mt-7 segmented"><button className={usePasskey?'selected':''} onClick={()=>setUsePasskey(true)}>Passkey</button><button className={!usePasskey?'selected':''} onClick={()=>setUsePasskey(false)}>Password</button></div>{usePasskey?<div className="mt-6"><PasskeyClient mode="login" onSuccess={()=>router.push('/dashboard')}/></div>:<form onSubmit={submit} className="mt-6 flex flex-col gap-4">{error&&<div className="rounded-lg border border-rose-500/30 bg-rose-500/10 p-3 text-sm text-rose-300">{error}</div>}<label className="field-label">Email<input name="email" type="email" required className="form-control" placeholder="you@example.com"/></label><label className="field-label">Password<div className="input-wrap"><input name="password" type={show?'text':'password'} required className="form-control border-0 px-0" placeholder="Enter your password"/><button type="button" className="icon-button" aria-label={show?'Hide password':'Show password'} onClick={()=>setShow(!show)}>{show?<EyeOff/>:<Eye/>}</button></div></label><label className="flex items-center gap-2 text-xs text-slate-400"><input type="checkbox" name="remember"/>Remember me</label><div className="flex items-center justify-between"><button className="primary-button flex-1" disabled={loading}>{loading?'Signing in...':'Sign In'}</button><Link href="/forgot-password" className="ml-4 text-xs text-amber-400 hover:text-amber-300">Forgot password?</Link></div></form>}<p className="mt-7 text-center text-sm text-slate-500">Don&apos;t have an account? <Link href="/register" className="text-amber-400 hover:text-amber-300">Create one</Link></p></div></div></section></main>}
