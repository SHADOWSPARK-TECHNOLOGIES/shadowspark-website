'use client'
import { useMemo, useState } from 'react'
import { format, subDays } from 'date-fns'
import { Download, SlidersHorizontal } from 'lucide-react'
import { toast } from 'sonner'
import { chartData } from '@/data/mock'
import { Bar, BarChart, CartesianGrid, Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis, AreaChart, Area } from 'recharts'
function exportAnalytics(){const csv=['Period,Volume,Repaid,Approved',...chartData.map(row=>`${row.day},${row.volume},${row.repaid},${row.approved}`)].join('\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv'}));a.download='shadowspark-analytics.csv';a.click();toast.success('Analytics exported as CSV')}
export default function AnalyticsPage(){const [range,setRange]=useState('30D');const [compare,setCompare]=useState(false);const [from,setFrom]=useState(format(subDays(new Date(),30),'yyyy-MM-dd'));const [to,setTo]=useState(format(new Date(),'yyyy-MM-dd'));const visible=useMemo(()=>range==='7D'?chartData.slice(-7):range==='90D'?chartData:chartData.slice(-30),[range]);return <div className="page-stack"><div className="page-intro"><div><p className="eyebrow">Portfolio intelligence</p><h2 className="page-heading">Analytics</h2><p className="page-lede">Understand loan volume, repayments, and operational throughput.</p></div><button className="secondary-button" onClick={exportAnalytics}><Download data-icon="inline-start"/>Export CSV</button></div><div className="panel flex flex-col gap-4 p-4 xl:flex-row xl:items-center"><div className="segmented"><button className={range==='1D'?'selected':''} onClick={()=>setRange('1D')}>Today</button><button className={range==='7D'?'selected':''} onClick={()=>setRange('7D')}>7D</button><button className={range==='30D'?'selected':''} onClick={()=>setRange('30D')}>30D</button><button className={range==='90D'?'selected':''} onClick={()=>setRange('90D')}>90D</button></div><div className="flex flex-wrap items-center gap-2"><label className="field-label">From<input type="date" className="form-control py-2" value={from} onChange={e=>setFrom(e.target.value)}/></label><label className="field-label">To<input type="date" className="form-control py-2" value={to} onChange={e=>setTo(e.target.value)}/></label></div><label className="ml-auto flex min-h-11 items-center gap-2 text-xs text-slate-300"><input type="checkbox" checked={compare} onChange={e=>setCompare(e.target.checked)}/>Compare to previous period</label><SlidersHorizontal className="hidden text-slate-500 xl:block"/></div>{compare&&<div className="pill-stat">Comparison enabled · Showing {format(new Date(from),'dd MMM yyyy')} — {format(new Date(to),'dd MMM yyyy')} against the previous period</div>}<div className="grid gap-6 lg:grid-cols-2"><div className="panel p-5 lg:col-span-2"><h3 className="font-semibold">Loan volume trend</h3><div className="mt-5 h-64"><ResponsiveContainer width="100%" height="100%"><AreaChart data={visible}><CartesianGrid stroke="#334155" strokeDasharray="3 3" vertical={false}/><XAxis dataKey="day" stroke="#64748b" tickLine={false}/><YAxis stroke="#64748b" tickLine={false}/><Tooltip contentStyle={{background:'#0f172a',border:'1px solid #334155',borderRadius:8}}/><Area type="monotone" dataKey="volume" stroke="#f59e0b" fill="#f59e0b" fillOpacity={.12}/>{compare&&<Area type="monotone" dataKey="approved" stroke="#64748b" fill="none" strokeDasharray="4 4"/>}</AreaChart></ResponsiveContainer></div></div><div className="panel p-5"><h3 className="font-semibold">Repayment performance</h3><div className="mt-5 h-56"><ResponsiveContainer width="100%" height="100%"><BarChart data={visible.slice(-10)}><CartesianGrid stroke="#334155" strokeDasharray="3 3" vertical={false}/><XAxis dataKey="day" stroke="#64748b"/><YAxis stroke="#64748b"/><Tooltip contentStyle={{background:'#0f172a',border:'1px solid #334155'}}/><Bar dataKey="repaid" fill="#10b981" radius={[3,3,0,0]}/><Bar dataKey="approved" fill="#f59e0b" radius={[3,3,0,0]}/></BarChart></ResponsiveContainer></div></div><div className="panel p-5"><h3 className="font-semibold">KYC processing time</h3><div className="mt-5 h-56"><ResponsiveContainer width="100%" height="100%"><LineChart data={visible}><CartesianGrid stroke="#334155" strokeDasharray="3 3" vertical={false}/><XAxis dataKey="day" stroke="#64748b"/><YAxis stroke="#64748b"/><Tooltip contentStyle={{background:'#0f172a',border:'1px solid #334155'}}/><Line type="monotone" dataKey="kyc" stroke="#38bdf8" strokeWidth={2}/></LineChart></ResponsiveContainer></div></div></div></div>}
