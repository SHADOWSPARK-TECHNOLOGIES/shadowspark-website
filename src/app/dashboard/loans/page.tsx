'use client'
import { useState } from 'react'
import { Download, Filter, Plus, Search } from 'lucide-react'
import { loans } from '@/data/mock'
import { LoanDetail, LoanTable, SectionHeader, StatusBadge } from '@/components/dashboard/components'
export default function LoansPage(){const [selected,setSelected]=useState(loans[0]);const [search,setSearch]=useState('');const filtered=loans.filter(l=>l.applicantName.toLowerCase().includes(search.toLowerCase())||l.id.toLowerCase().includes(search.toLowerCase()));return <div className="page-stack"><div className="page-intro"><div><p className="eyebrow">Portfolio management</p><h2 className="page-heading">Loan applications</h2><p className="page-lede">Review, approve, and manage every application.</p></div><div className="flex gap-2"><button className="secondary-button"><Download data-icon="inline-start"/>Export</button><button className="primary-button"><Plus data-icon="inline-start"/>New application</button></div></div><div className="panel p-4"><div className="flex flex-col gap-3 lg:flex-row"><div className="input-wrap flex-1"><Search/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search applicant or loan ID" aria-label="Search loans"/></div><button className="secondary-button"><Filter data-icon="inline-start"/>Filters</button><select className="select-control" aria-label="Filter by status"><option value="">All statuses</option><option>Pending KYC</option><option>Approved</option><option>Disbursed</option></select></div></div><div className="panel p-5"><SectionHeader title={`${filtered.length} applications`} description="Sorted by newest first" action={<span className="text-xs text-slate-500">Select a row to inspect</span>}/><LoanTable rows={filtered} onSelect={setSelected}/></div>{selected&&<LoanDetail loan={selected} onClose={()=>setSelected(undefined as never)}/>}</div>}
