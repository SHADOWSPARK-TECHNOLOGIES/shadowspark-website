'use client'
import { useState } from 'react'
export default function SettingsPage(){const [ndpr,setNdpr]=useState(true);return <div className="page-stack"><div><p className="eyebrow">Workspace administration</p><h2 className="page-heading">Settings</h2></div><div className="grid gap-6 lg:grid-cols-2"><div className="panel p-5"><h3 className="font-semibold">Tenant profile</h3><div className="mt-5 flex flex-col gap-4"><label className="field-label">Organisation name<input className="form-control" defaultValue="Lendwise Nigeria"/></label><label className="field-label">Tenant slug<input className="form-control" defaultValue="lendwise-ng"/></label><button className="primary-button self-start">Save changes</button></div></div><div className="panel p-5"><h3 className="font-semibold">Compliance</h3><div className="mt-5 flex items-center justify-between rounded-lg bg-slate-900 p-4"><div><p className="font-medium">NDPR controls</p><p className="mt-1 text-xs text-slate-500">Enforce retention and consent workflows.</p></div><button onClick={()=>setNdpr(!ndpr)} className={`toggle ${ndpr?'toggle-on':''}`} aria-label="Toggle NDPR controls"><span/></button></div></div><div className="panel p-5 lg:col-span-2"><h3 className="font-semibold">Team members</h3><div className="mt-4 overflow-x-auto"><table className="data-table"><tbody>{[['Stephen Okoro','stephen@lendwise.ng','Administrator'],['Amaka Okafor','amaka@lendwise.ng','Manager'],['Tunde Akinyemi','tunde@lendwise.ng','Agent']].map(row=><tr key={row[1]}>{row.map((cell,i)=><td key={cell} className={i===2?'text-slate-500':''}>{cell}</td>)}</tr>)}</tbody></table></div></div></div></div>}
