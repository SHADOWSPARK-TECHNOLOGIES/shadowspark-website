import { auditEvents } from '@/data/mock'
import { relativeDate } from '@/lib/formatters'
export default function AuditLogsPage(){return <div className="page-stack"><div><p className="eyebrow">Governance</p><h2 className="page-heading">Audit logs</h2><p className="page-lede">Immutable record of every operational change.</p></div><div className="panel p-5"><div className="flex flex-col gap-4">{auditEvents.map(event=><div className="flex items-start gap-4 border-b border-slate-800 pb-4 last:border-0 last:pb-0" key={event.id}><div className="mt-1 size-2 rounded-full bg-amber-400"/><div className="min-w-0 flex-1"><p className="font-medium text-slate-200">{event.action}</p><p className="mt-1 text-sm text-slate-500">{event.detail}</p><p className="mt-2 text-xs text-slate-600">{event.actor} · {relativeDate(event.createdAt)} · {event.loanId}</p></div></div>)}</div></div></div>}
