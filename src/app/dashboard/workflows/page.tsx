import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Workflows — ShadowSpark",
  description: "Lending process automation",
};

export default function WorkflowsPage() {
  return (
    <div className="dashboard-card">
      <h2>Workflows</h2>
      <p>Workflow automation and process management coming soon.</p>
    </div>
  );
}
