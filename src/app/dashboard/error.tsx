'use client'
export default function DashboardError({reset}:{reset:()=>void}){return <div className="page-stack"><div className="panel flex flex-col items-center py-20 text-center"><h2 className="text-xl font-semibold">Something went wrong</h2><p className="mt-2 text-sm text-slate-500">We could not load this workspace surface.</p><button className="primary-button mt-6" onClick={reset}>Try again</button></div></div>}
