export class ApiError extends Error { constructor(public status:number, message:string){super(message)} }
export async function apiClient<T>(path:string, options:RequestInit={}) { const headers=new Headers(options.headers); if(typeof window!=='undefined'){ const token=window.localStorage.getItem('shadowspark_token'); if(token) headers.set('Authorization',`Bearer ${token}`) } headers.set('X-Tenant-Slug', typeof window!=='undefined'?window.location.hostname.split('.')[0]:'demo'); if(options.method && options.method!=='GET') headers.set('Idempotency-Key',crypto.randomUUID()); const response=await fetch(`${process.env.NEXT_PUBLIC_API_URL ?? ''}${path}`,{...options,headers}); if(!response.ok) throw new ApiError(response.status, await response.text() || 'Request failed'); return response.json() as Promise<T> }
