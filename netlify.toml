[build]
  command = "pnpm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "24"
  PNPM_FLAGS = "--shamefully-hoist --frozen-lockfile"

[[plugins]]
  package = "@netlify/plugin-nextjs"
